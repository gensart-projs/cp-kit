# Copilot Instructions (repo-level)

These are global guidelines for Copilot behaviors in this repository. This file must exist for `cp-toolkit add` to work in a project.

- Always load `AGENTS.md` and `.github/agents` entries
- Prefer safe defaults for security and privacy
- Respect the project's instruction files in `.github/instructions`
